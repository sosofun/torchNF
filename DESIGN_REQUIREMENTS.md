# 基于 PyTorch DTensor 的分布式大模型训练框架
# 设计需求说明书（面向算法研究员）

## 1. 文档目的
本说明书用于定义一个基于 PyTorch 原生 DTensor 的分布式大模型训练框架的详细设计需求。目标用户是算法研究员，要求研究友好、易用、可扩展，并兼具高性能，支持数据并行、张量并行、流水线并行、上下文并行、专家并行等多种并行方式及其组合。

## 2. 适用范围
- 适用于大规模 Transformer、MoE、检索增强、长上下文模型等训练。
- 适用于多机多卡、异构算力（GPU/CPU）集群环境。
- 适用于研究原型、实验复现、快速迭代及部分生产级训练任务。

## 3. 术语定义
- **DTensor**：PyTorch 原生分布式张量，支持分片/副本布局。
- **Mesh**：设备网格拓扑，描述并行度及设备映射。
- **Layout/Placement**：张量在 Mesh 上的布局与分片方式。
- **DP**（数据并行）、**TP**（张量并行）、**PP**（流水线并行）、
  **CP**（上下文并行）、**EP**（专家并行）。

## 4. 总体目标
1. 提供研究友好、可组合的并行训练框架，降低并行配置复杂度。
2. 充分利用 PyTorch DTensor 的原生能力，减少自研通信栈。
3. 在吞吐、显存、可扩展性等方面提供稳定高性能。
4. 支持多种并行方式组合（如 2D/3D 并行、DP+TP+PP+EP+CP）。

## 5. 非目标
1. 不追求替代 PyTorch 生态（只扩展并行训练能力）。
2. 不提供完整自动并行搜索（仅提供规划建议和启发式）。
3. 不强绑定特定模型结构（以通用算子与模块支持为主）。

## 6. 目标用户与核心使用场景
### 6.1 目标用户
- 算法研究员、模型研究工程师、快速实验的研发人员。

### 6.2 使用场景
- 快速将单机模型升级为多机多卡分布式训练。
- 尝试不同并行策略组合，以优化性能与显存。
- 研究新结构（如 MoE、长上下文）并进行规模化训练。

## 7. 功能需求（Functional Requirements）

### 7.1 并行策略支持
1. **数据并行（DP）**
   - 支持参数、梯度、优化器状态的分布式同步。
   - 支持 ZeRO 风格的参数/梯度/优化器分片（可选）。
2. **张量并行（TP）**
   - 支持 Transformer 常见算子分片（Linear、Attention、MLP）。
   - 支持列并行、行并行、序列并行等经典方案。
3. **流水线并行（PP）**
   - 支持多 stage 切分与自动分配。
   - 支持 1F1B、GPipe 等调度。
   - 支持激活重计算与微批调度。
4. **上下文并行（CP）**
   - 支持长序列切分与跨设备注意力计算。
   - 兼容 RoPE、ALiBi 等位置编码策略。
5. **专家并行（EP）**
   - 支持 MoE 路由、负载均衡、容量控制。
   - 支持 Top-1/Top-2 路由与分布式 All-to-All。
6. **并行组合**
   - 支持 DP+TP、DP+PP、DP+TP+PP、TP+EP、CP+TP+DP 等多维组合。
   - 提供冲突检测与布局兼容性提示。

### 7.2 分布式张量与布局管理
- 统一的 DTensor 布局 API，支持 Shard、Replicate、Partial。
- 提供布局推断与自动传播机制。
- 提供布局可视化/调试工具。

### 7.3 易用性与研究友好性
1. **最小侵入式 API**
   - 支持从 torch.nn.Module 平滑迁移。
2. **策略配置**
   - YAML/JSON + Python API 配置并行策略。
3. **模块化可插拔**
   - 可替换通信、调度器、路由器等组件。
4. **可视化与调试**
   - 并行拓扑、layout 可视化。
   - 统计通信开销、显存与吞吐。

### 7.4 训练引擎
- 提供统一训练引擎与简洁训练循环。
- 支持混合精度（AMP/BF16/FP8 可扩展）。
- 支持梯度累积、梯度裁剪、激活重计算。
- 支持检查点恢复与自动保存。

### 7.5 容错与恢复
- 支持训练中断恢复（故障重启恢复最近 checkpoint）。
- 支持容错策略配置（保存频率、策略可插拔）。

### 7.6 生态与兼容
- 兼容 HuggingFace、TorchVision、Triton 等常用生态。
- 提供适配层以兼容自定义模型。

## 8. 非功能性需求（Non-Functional Requirements）

### 8.1 性能要求
- 吞吐率可接近手工并行优化方案。
- 通信与计算重叠，减少 idle。
- 保证大规模扩展下性能可线性提升到一定规模。

### 8.2 可扩展性
- 支持从 1 机到 1000+ GPU 规模。
- 支持不同拓扑 Mesh（1D/2D/3D）。

### 8.3 可维护性
- 核心模块清晰解耦（并行规划、执行引擎、通信层、调度层）。
- 代码风格一致，提供详细文档与示例。

### 8.4 可测试性
- 提供单元测试、并行组合测试、性能回归测试。

## 9. 架构设计需求

### 9.1 模块分层
1. **Parallel API 层**
   - 用户接口（Python API、配置文件）。
2. **Planner/Strategy 层**
   - 并行策略组合、布局推断与验证。
3. **Runtime 层**
   - 执行图构建、通信调度、流水线调度。
4. **Communication 层**
   - NCCL/Collective，支持 all-reduce、all-to-all 等。
5. **Model Adapter 层**
   - 对常见模型结构提供内置支持与模板。

### 9.2 DTensor 集成
- 使用 DTensor Layout 表示分片策略。
- 在模块转换阶段生成 DTensor 化权重与输入输出。
- 关键算子使用 DTensor-aware kernel。

### 9.3 并行策略组合逻辑
- 提供策略兼容矩阵与约束规则。
- 明确不同维度之间的 Mesh 划分与映射。

## 10. 关键特性设计需求

### 10.1 通信优化
- 支持 NCCL 优化路径。
- 支持梯度压缩（可选）。
- 支持异步通信/计算重叠。

### 10.2 Pipeline 计划
- 提供多种调度策略。
- 支持显存优化策略（激活重算与分阶段卸载）。

### 10.3 MoE 专家并行
- 支持动态路由与专家负载均衡。
- 支持路由器可插拔与策略实验。

### 10.4 上下文并行
- 提供长序列切分策略，支持 attention 跨分片计算。
- 支持可变长度序列与 padding 优化。

## 11. 用户接口需求
### 11.1 示例 API
```python
from torchnf import ParallelConfig, distribute, Trainer

config = ParallelConfig(
    mesh="dp=4,tp=2,pp=2,cp=1,ep=2",
    tp_style="tensor",
    pipeline_schedule="1f1b",
    moe_top_k=2
)

model = build_model()
model = distribute(model, config)

trainer = Trainer(config)
trainer.fit(model, dataloader)
```

### 11.2 研究友好要求
- 配置可热更新与记录到实验日志。
- 支持自动记录并行配置与版本信息。
- API 与配置尽量简洁，减少必须代码改动。

## 12. 性能与评测指标
- 吞吐（tokens/s）。
- 加速比与扩展效率（scaling efficiency）。
- GPU 利用率与通信占比。
- 显存占用峰值。

## 13. 兼容性与约束
- PyTorch >= 2.x。
- CUDA >= 12.x（可扩展）。
- 兼容常见 GPU（A100、H100、L40 等）。

## 14. 测试与验证需求
- 功能测试：DP/TP/PP/CP/EP 单独与组合。
- 性能测试：不同规模扩展与瓶颈分析。
- 数值一致性测试：与单机 FP32 结果对齐。
- 故障恢复测试：模拟节点中断与恢复。

## 15. 交付物
- 设计文档与 API 文档。
- 示例模型与训练脚本。
- 基准性能报告。

